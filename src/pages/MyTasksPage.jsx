import React from 'react'

const MyTasksPage = () => {
  return (
    <div>
      // tabs for total tasks, completed tasks, and pending tasks, overDue tasks
      // search bar to search tasks
      //upcoming tasks -- which are due in next 7 days
      // tasks with due date -- tasks with due date
      // 
    </div>
  )
}

export default MyTasksPage
